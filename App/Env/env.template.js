// FILL IN VALUES, and RENAME TO env.js in this directory!
const config = {
    ENVIRONMENT: 'DEV',
    API_URL: 'https://dev.transformationnb.ca/',
    // Secrets
    API_TOKEN_NAME: 'X-COVAULT-API-TOKEN',
    API_TOKEN: '',
    API_TIMEOUT: 2000,
}

module.exports = {
    config: config,
}
